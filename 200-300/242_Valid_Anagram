/*给两个字符串s和t问是不是变位词，变位词就是同一个单词交换字母顺序。我直接调用sort对字符串内部排序，最后s和t相等就是变位词*/
class Solution {
public:
    bool isAnagram(string s, string t) {
        sort(s.begin(),s.end());
        sort(t.begin(),t.end());
        if(s==t) return true;
        else return false;
        
    }
};
